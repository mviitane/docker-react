language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t mviitane/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mviitane/docker-react npm run test
